<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdservice.mapper.SysLogMapper">
    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, username, module, operation, description, request_url,
        request_method, method, params, time_cost, result, exception, create_time
    </sql>

    <!-- 分页查询日志 -->
    <select id="selectSysLogList" resultType="com.mdservice.entity.SysLog">
        SELECT
        <include refid="Base_Column_List"/>
        FROM sys_operation_log
        WHERE 1=1
        <!-- 模糊查询用户名 -->
        <if test="username != null and username != ''">
            AND username LIKE CONCAT('%', #{username}, '%')
        </if>
        <!-- 模糊查询模块 -->
        <if test="module != null and module != ''">
            AND module LIKE CONCAT('%', #{module}, '%')
        </if>
        <!-- 按操作时间倒序 -->
        ORDER BY create_time DESC
    </select>
</mapper>