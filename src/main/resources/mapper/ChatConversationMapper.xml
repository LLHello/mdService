<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mdservice.mapper.ChatConversationMapper">

    <select id="selectByUsers" resultType="com.mdservice.entity.ChatConversation">
        select id,
               user1_id,
               user2_id,
               last_message_id,
               last_message_time,
               create_time,
               update_time
        from chat_conversation
        where user1_id = #{user1Id}
          and user2_id = #{user2Id}
        limit 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into chat_conversation(user1_id, user2_id, last_message_id, last_message_time, create_time, update_time)
        values (#{user1Id}, #{user2Id}, #{lastMessageId}, #{lastMessageTime}, now(), now())
    </insert>

    <update id="updateLastMessage">
        update chat_conversation
        set last_message_id   = #{lastMessageId},
            last_message_time = #{lastMessageTime},
            update_time       = now()
        where id = #{id}
    </update>

</mapper>
